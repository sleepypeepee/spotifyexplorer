var path = '/';

// OPTIONAL - If deploying to a path other than root '/' then specify the path here
// if (process.env.NODE_ENV === 'production') {
// 	path = '/spotifyexplorer/';
// }
// if (process.env.NODE_ENV === 'test') {
// 	path = '/spotifyexplorer/dev/';
// }

module.exports = {
  publicPath: path
}